DATABASE_URL=postgresql+psycopg://certus:certus@localhost:5432/certus
TOLERANCIA_BALANCE_TON=0.006

SESSION_SECRET=change-me
# Uno o más orígenes separados por coma (frontend local, GitHub Pages, etc.)
FRONTEND_URL=http://localhost:5173
ALLOWED_EMAIL_DOMAIN=friosur.cl
# true en producción cuando frontend y backend están en dominios distintos
# (necesario para que la cookie de sesión funcione cross-origin)
SESSION_COOKIE_CROSS_SITE=false
